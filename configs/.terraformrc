plugin_cache_dir   = "$HOME/.terraform.d/plugin-cache"
# Explicit provider installation config to exclude local mirrors so that other jobs will not interfere with provider versions.
# Used mostly only in shared CI environment
provider_installation {
  direct {
    include = ["*/*/*"]
  }
}
